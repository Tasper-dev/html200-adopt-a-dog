/* Adopt Dog Button */
function dogDetails(dogName, dogBreed, adoptFee) {
  result = dogName + " is " + dogBreed + "." + " Adoption fee is " + adoptFee;
  alert(result);
}

function addFee(adoptFee) {
  result = adoptFee + alert(result);
}

let total = 0;

function addFee(adoptFee) {
  total += adoptFee;
  alert("Total is: $" + total.toFixed(2));
}
